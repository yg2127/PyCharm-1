sorting
정렬 (134689)
Insertion sorting
pseudocode는 언어에 따라 다르지 않음
Insertion sorting은 다른 배열들이 필요하지 않다.
// Insertion sorting's pseudo code

Insertion Sort(A,n) // A배열에 n개의 숫자가 있다는 뜻
for i = 2 to n
    key = A[i] // insert A[i] into the sorted subarray A[1:i-1]
    j = i-1
    while j > 0 and A[j] > key
        a[j+1] = A[j]
        j = j-1
    A[j+1] = key

시간복잡도에 유의하면서 사용해야함

Merge sort
